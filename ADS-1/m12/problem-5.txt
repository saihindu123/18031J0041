Learining by doing-2

Is an array that is sorted in Ascending order a min-oriented heap? Why?
Yes,because root element is minimum in minheap, and the childs will be maximum when compared to parent. so as it is sorted in ascending 
order then it would be min-oriented heap.
